// Main entry - renders the AppShell which handles all client-side routing.
// See the fullstack-dev skill: only `/` route is exposed in preview, so we
// manage navigation client-side via hash routing.

export default function Home() {
  return null;
}
